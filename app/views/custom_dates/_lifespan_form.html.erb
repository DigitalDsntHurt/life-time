<%= form_with(model: @custom_date, local: true) do |form| %>
  <% if @custom_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_date.errors.count, "error") %> prohibited this custom_date from being saved:</h2>

      <ul>
        <% @custom_date.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table class="form-table">
    <tr>
      <td>
        <div class="field">
          <%= form.label :bday %>
          <%= form.date_field :bday %>
        </div>
      </td>
      <td>
        <div class="field">
          <%= form.label :dday %>
          <%= form.date_field :dday %>
        </div>    
      </td>
      <td>
        <div class="actions">
          <%= form.submit %>
        </div>
      </td>
    </tr>
  </table>
<% end %>
